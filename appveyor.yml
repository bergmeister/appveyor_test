image: Ubuntu

build_script:
  - ps: Invoke-WebRequest 'https://dot.net/v1/dotnet-install.sh' -OutFile dotnet-install.sh;  $installDir =  $env:PATH.Split([System.IO.Path]::PathSeparator) | Select-Object -Last 1; Write-Verbose "inst $installDir" -Verbose; $env:DOTNET_INSTALL_DIR = "$pwd\.dotnetsdk"; ./dotnet-install.sh --version $dotNetCoreSDKVersion; $env:Path = "$env:DOTNET_INSTALL_DIR;$env:Path"
  - ps: $PSVersionTable; dotnet --version